I will begin by investigating the current state of the repository, focusing on the local loopback API implementation and the files currently undergoing refactoring to understand the delta between the fork and upstream.
I will now check for the existence of `ui/history` and the contents of `data/chathistory` to confirm the progress of the chat history de-scoping.
I will now check the contents of `ui/history` to confirm if it has been cleared as part of the chat history de-scoping.
I'll now examine `LlmHttpService.kt` to understand the loopback API implementation, which is a critical, non-negotiable component of the project.
`LlmHttpService` relies on `LlmChatModelHelper` for inference, so I'll examine `LlmChatModelHelper.kt` and `LlmChatViewModel.kt`. I'll also check `Android/src/app/src/main/java/com/google/ai/edge/gallery/data/LlmHttpPrefs.kt` to understand the service's configuration.
I'll examine `LlmChatModelHelper.kt` to see how "thinking mode support" and "max-token/config dialog improvements" from upstream are being integrated into the `LlmChatModelHelper`.
In `LlmChatModelHelper.kt`, "thinking" references exist but aren't fully integrated into `runInference`. I'll now examine `LlmChatViewModel.kt` to see how it handles inference and if it utilizes the `partialThinkingResult`.
`LlmChatViewModelBase` is set up to handle thinking results, but `LlmChatModelHelper.kt` currently passes `null` for them. I suspect a newer LiteRT LM version might offer a different way to retrieve these results. I'll now examine `GalleryNavGraph.kt` to see how chat history is being removed.
`GalleryNavGraph.kt` uses `CustomTaskScreen` and `CustomTaskData`, but I don't see `LlmChatScreen` mentioned, despite its presence in `ui/llmchat`. Given the user's instructions to de-scope chat history and unwire references, I need to investigate if `LlmChatScreen` is still in use, possibly within a `CustomTask`. I'll start by looking for Tiny Garden's implementation, perhaps in `TinyGardenTask.kt`.
Tiny Garden is restored. Now, I need to check if there's an `LlmChatTask.kt` for the main Chat feature.
`LlmChatTaskModule.kt` is present; I'll examine its contents.
The chat features are now encapsulated as `CustomTask`s, a major architectural shift. My plan involves four phases: stabilization and hardening of the current delta, core runtime and model updates, UI/UX parity, and local API hardening. I'll prioritize finishing the thinking mode integration and ensuring the `LlmHttpService` correctly handles new configurations. I recommend a "themed cherry-pick" strategy for syncing, with clear verification gates at each phase. I'll now examine `Config.kt` to assess its current state and identify any necessary foundational conflicts to resolve.
To catch up the Android repo with `upstream/main` while preserving the local Every Code HTTP API and de-scoped chat history, I've developed the following phased implementation plan. This plan prioritizes stabilizing the current in-flight refactors before moving to broader upstream integration.

### **Phase 1: Stabilization of Current Integration (The "Thinking" Delta)**
**Goal:** Finalize the partially integrated "thinking mode" and config plumbing to establish a stable baseline.

1.  **Thinking Result Plumbing:** Update `LlmChatModelHelper.runInference` to correctly pass through `partialThinkingResult`. This may require updating the LiteRT-LM `MessageCallback` implementation if upstream has introduced a specific "thinking" stream or tag-based parsing.
2.  **ViewModel Sync:** Ensure `LlmChatViewModel` correctly handles the transition from `ChatMessageThinking` to `ChatMessageText`.
3.  **Config Logic Standardization:** Standardize the logic in `Config.kt` and `ConfigDialog.kt` for the new `max-token` slider vs. label behavior. Verify that `ENABLE_THINKING` state is correctly persisted and passed to the inference engine.
4.  **Verification Gate:** App builds successfully; "AI Chat" task functions with thinking mode enabled (if supported by the model).

### **Phase 2: Foundation & Local API Hardening**
**Goal:** Align core data structures with upstream while insulating the non-negotiable HTTP API.

1.  **Core Data Sync:** Sync `Model.kt` and `Config.kt` with upstream. 
    *   **Hotspot:** Resolve conflicts in `Model` properties. Preserve any custom fields added for `LlmHttpService` model resolution.
2.  **Model Allowlist Reconciliation:** Merge upstream `ModelAllowlist.kt` and `model_allowlist.json`. 
    *   **Constraint:** Maintain support for local overrides (e.g., the `__imports` directory logic) used by the HTTP service.
3.  **HTTP API Robustness:** Refactor `LlmHttpService.kt` to use the updated `LlmChatModelHelper` APIs. Ensure the service handles new model configuration parameters (like `thinking` mode) by either ignoring them for raw completion or providing sensible fallbacks.
4.  **Verification Gate:** `LlmHttpService` responds to `/ping` and `/generate` on `127.0.0.1:9006`.

### **Phase 3: Modernization & Feature Parity**
**Goal:** Adopt improved upstream UI/UX components and remaining feature blocks.

1.  **Audio & Image Task Refinement:** Sync `LlmAskAudioTask` and `LlmAskImageTask` with upstream refinements. This includes audio-only message support and improved prompt handling for multimodal models.
2.  **UI Component Adoption:** Integrate upstream improvements to the `ConfigDialog` and theme-related updates. Ensure these components respect the current de-scoped state (no history).
3.  **Final History Unwiring:** Perform a final audit of DI (Hilt) modules and `GalleryNavGraph.kt` to ensure all `data/chathistory` and `ui/history` references are fully removed and not just commented out.
4.  **Verification Gate:** "Ask Audio" and "Ask Image" tasks are functional and visually consistent with upstream.

### **Phase 4: Cleanup & CI Validation**
**Goal:** Finalize the catch-up and establish automated guards for the local API.

1.  **Codebase Audit:** Remove any legacy "just-in-case" code from previous versions that was bypassed during the `CustomTask` transition.
2.  **Automated Validation:** Update `.github/workflows/android-apk.yml` to include a unit test run. 
    *   **Recommendation:** Add a simple local test for `LlmHttpService` that verifies the JSON schema of its responses remains stable.
3.  **Documentation:** Update `README.md` and `IMPLEMENTATION_PLAN.md` to reflect the new `CustomTask` architecture and the preserved HTTP API endpoints.

---

### **Strategic Recommendations**

*   **Sync Strategy:** Use **Themed Cherry-picks**. Avoid a giant `git merge upstream/main` which will likely cause catastrophic conflicts in the de-scoped areas. Identify upstream commits by feature block (e.g., "Thinking mode improvements") and apply them sequentially.
*   **Checkpoint Structure:** Commit after every successful Verification Gate. If a sync breaks the `LlmHttpService`, backtrack immediately.
*   **Conflict Hotspots:**
    *   `Config.kt` & `Model.kt`: Foundation for all tasks.
    *   `LlmChatModelHelper.kt`: The bridge that both the UI and the HTTP Service rely on.
    *   `GalleryNavGraph.kt`: Routing logic for custom tasks.
*   **Rollback Checkpoint:** Before starting Phase 2, create a stable tag or branch (`stabilized-delta`). This ensures a safe return point if the foundational sync introduces regressions in the LiteRT-LM integration.
