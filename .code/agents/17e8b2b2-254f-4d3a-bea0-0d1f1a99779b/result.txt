I've created a comprehensive implementation plan at `/home/zyach/projects/gallery/UPSTREAM_CATCHUP_PLAN.md`. Here's a summary:

## Key Highlights

### Sync Strategy Recommendation
**File-by-file adoption** (NOT merge-based) due to:
- Dirty tree with in-flight chat history removal
- HTTP API is fork-critical and must be preserved
- ~30+ commits behind with complex divergence

### Phase Structure (7 Phases)

| Phase | Focus | Risk Level |
|-------|-------|------------|
| **0** | Stabilize in-flight work (chat history removal, thinking mode) | CRITICAL |
| **1** | HTTP API hardening + test suite | CRITICAL |
| **2** | Upstream Batch 1: Low-risk files (utils, analytics, runtime) | LOW |
| **3** | Upstream Batch 2: Core files (Model.kt, ModelAllowlist.kt, Config.kt) | MEDIUM |
| **4** | Upstream Batch 3: UI files (ConfigDialog, ChatPanel, LlmChatViewModel) | HIGH |
| **5** | Upstream Batch 4: Features (audio-only, max-token dialog, tools migration) | MEDIUM |
| **6** | Final testing & reconciliation | - |
| **7** | Documentation & maintenance strategy | - |

### Conflict Hotspots (Critical Files)
1. **`LlmHttpService.kt`** - Fork-only, do not overwrite
2. **`LlmHttpPrefs.kt`** - Fork-only, do not overwrite
3. **`LlmChatViewModel.kt`** - Chat history removal divergence
4. **`LlmChatModelHelper.kt`** - HTTP API init path + upstream improvements
5. **`ConfigDialog.kt`** - Thinking toggle + upstream UI refinements

### Verification Gates
- `./gradlew :app:assembleDebug` after each phase
- GitHub Actions APK build validation
- Manual testing matrix for HTTP API endpoints + thinking mode

### Estimated Timeline: 25-39 hours (spread over 1-2 weeks)

The plan includes rollback checkpoints, a detailed testing matrix, and recommendations for future upstream sync cadence (monthly recommended).
