I will begin by investigating the current state of the repository, focusing on the local HTTP API modifications and the chat history subsystem to understand their integration points and dependencies.
I will now examine `LlmHttpService.kt` to understand the local HTTP API's implementation and how it's integrated via `AppModule.kt`.
`LlmHttpService` utilizes `NanoHTTPD` for local API serving and `LlmChatModelHelper` for inference. I'll now examine `LlmChatModelHelper` and `LlmModelInstance` in `ui/llmchat/`.
I'll examine `LlmChatModelHelper.kt` to determine its coupling with the UI and whether `LlmHttpService` can use it independently.
`LlmChatModelHelper` provides inference helpers using `LiteRT LM`. I'll now investigate the chat history subsystem, specifically `ChatHistoryRepository.kt`, `ui/history/`, and its integration in `AppModule.kt` by searching for `ChatHistory` references.
Chat history is deeply integrated across `data/chathistory/`, `di/AppModule.kt`, `ui/history/`, `ui/navigation/GalleryNavGraph.kt`, `ui/llmchat/LlmChatViewModel.kt`, and `ui/home/HomeScreen.kt`. I'll now review the `README.md` to understand the fork's documented goals.
The README notes "First steps to implement chat history support [in process]", but the user has now deprecated this. "Every Code" likely refers to a local tool utilizing the phone's LLM via the HTTP API. I'll now check `git log upstream/main -n 5` to identify recent upstream changes.
Upstream hit `1.0.10` and is moving fast. I'll check the fork's `build.gradle` to find its current version.
Fork's at 1.0.9, upstream's at 1.0.10. I'll compare the two in upstream.
Upstream 1.0.10 brings massive changes, including a new Benchmark feature, major `HomeScreen` and `GalleryNavGraph` updates, and a `ModelImportDialog.kt` relocation that will clash with fork modifications. Given the user's request to drop the chat history subsystem, I'll examine the specific additions in `AppModule.kt` related to it.
Time to compare `AppModule.kt` with upstream 1.0.10.
Upstream's `AppModule.kt` has pivoted to `DataStore`, while the fork's `ChatHistory` additions linger. Removing chat history entails deleting `data/chathistory/` and `ui/history/`, purging references from `AppModule.kt`, `GalleryNavGraph.kt`, and `HomeScreen.kt`, and refactoring `LlmChatViewModel.kt` for ephemeral chat. I'll now examine `LlmHttpPrefs.kt`, as `LlmHttpService.kt` relies on it alongside `LlmChatModelHelper` and `LlmModelInstance`.
`LlmHttpPrefs` relies on `SharedPreferences`, diverging from upstream's `DataStore` trend. While I should eventually migrate them, for now, I'll just purge `KEY_HISTORY_ENABLED` and its methods. Next, I'll compare the fork's `GalleryNavGraph.kt` with upstream to see how they've diverged.
`GalleryNavGraph.kt` contains `ROUTE_CHAT_HISTORY` and a `sessionId` parameter in the model route; both need to go. Next up: `LlmChatViewModel.kt`.
`LlmChatViewModelBase` depends too much on `ChatHistoryRepository`. I'll strip the repository from the constructor, purge session-related methods like `initializeSession` and `persistMessages`, and refactor `resetSession`. `ChatViewModel` likely handles in-memory message management, so I'll verify that next.
`ChatViewModel` uses `MutableStateFlow` for message management, so removing `ChatHistoryRepository` simply means chat won't persist, aligning with the user's request. 

I've developed a four-phase plan: first, establish a baseline by merging `upstream/main` and resolving conflicts in `AppModule.kt` and `HomeScreen.kt`; second, purge the chat history subsystem from data, UI, and navigation; third, harden the local HTTP API and integrate it with new upstream features; and finally, validate everything and implement a `fork-main` branch strategy to simplify future rebases. Major conflict zones include `AppModule.kt`, `HomeScreen.kt`, and `DataStoreRepository.kt`. I'll now flesh this out into a detailed plan.
Plan's done, but I'm doing a final check for chat history remnants. Checking `LlmChatTaskModule.kt` in `ui/llmchat/` now.
`LlmChatTaskModule.kt` is passing `sessionId` to screens; that needs to go. Checking `LlmChatScreen.kt` next.
`ChatViewWrapper` is cluttered with `sessionId` and session-related ViewModel calls. My plan's solid, but I'll add a specific task to Phase 2 for cleaning up `LlmChatScreen.kt` and `LlmChatTaskModule.kt`.
Plan's good. Final check: `DataStoreRepository.kt` in the fork.
Comparing this to upstream `DataStoreRepository.kt` now.
Upstream's `DataStoreRepository` has ballooned with `Benchmark`, `Cutouts`, and `Secrets` support, while the fork's version is trailing. The constructor differences confirm a major clash in `AppModule.kt`. I'm ready to move forward.
